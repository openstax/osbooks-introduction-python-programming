window.parameters = {"files": [{"name": "cylinder.py", "contents": "# TODO: Add the import statement.\n\nr = float(input(\"radius? \"))\nh = float(input(\"height? \"))\n\narea = 0    # TODO: Replace with area formula.\nvolume = 0  # TODO: Replace with volume formula.\n\nprint(\"Area:\", round(area, 2))\nprint(\"Volume:\", round(volume, 2))\n", "solution": "import math\n\nr = float(input(\"radius? \"))\nh = float(input(\"height? \"))\n\narea = (math.tau * r*h) + (math.tau * r**2)\nvolume = math.pi * r**2 * h\n\nprint(\"Area:\", round(area, 2))\nprint(\"Volume:\", round(volume, 2))\n"}], "input": "0\n0\n", "showSolution": true};