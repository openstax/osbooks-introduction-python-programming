window.parameters = {"files": [{"name": "main.py", "contents": null, "solution": "# Define the recursive function to calculate the summation\ndef digit_sum(n):\n    if n < 0:\n        print(\"The summation is not defined for negative integers.\")\n        return -1\n    elif n < 10:           # Base case for single digit numbers\n        return n\n    else:\n        return n%10 + digit_sum(n//10)\n    \n# Take the input from the user\nn = int(input())\n\n# Call the recursive function\nprint(digit_sum(n))\n"}], "input": "6721", "showSolution": true};