window.parameters = {"files": [{"name": "main.py", "contents": "\n", "solution": "# Define the recursive function to calculate the summation\ndef rec_sum(n):\n    if n < 0:\n        print(\"The summation is not defined for negative integers.\")\n        return -1\n    elif n == 0:           # Base case\n        return 0\n    else:\n        return n + rec_sum(n-1)\n\n# Take the input from the user\nn = int(input())\n\n# Call the recursive function\nprint(rec_sum(n))\n\n"}], "input": "5", "showSolution": true};