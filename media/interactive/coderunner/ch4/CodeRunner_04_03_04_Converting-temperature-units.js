window.parameters = {"files": [{"name": "main.py", "contents": "temp = float(input())\nunit = input() # f for Fahrenheit or c for Celsius\n\nprint(temp, unit, \"is converted to\", new_temp, new_unit)", "solution": "temp = float(input())\nunit = input() # f for Fahrenheit or c for Celsius\n\nif unit == \"f\":\n    new_temp = (temp-32) * 5/9\n    new_unit = \"c\" \nelse:\n    new_temp = (temp * 9/5) + 32\n    new_unit = \"f\"\n\nprint(temp, unit, \"is converted to\", new_temp, new_unit)"}], "input": "68\nf\n", "showSolution": true};