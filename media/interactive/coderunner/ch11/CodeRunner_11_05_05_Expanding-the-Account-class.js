window.parameters = {"files": [{"name": "main.py", "contents": "class Account:    \n    def __init__(self, name=\"\", amount=0): \n        self.name = name\n        self.amount = amount    \n\n    def __str__(self):\n        return f\"{self.name}: ${self.amount}\"\n\n    def __add__(self, other):\n        # Add code to allow adding int values too\n        return Account(self.name + \", \" + other.name,\n                       self.amount + other.amount)\n\nname_a = input()\namt_a = int(input())\nacct_a = Account(name_a,  amt_a)\nname_b = input()\namt_b = int(input())\nacct_b = Account(name_b, amt_b)\n\nacct_ab = acct_a + acct_b # Combine accounts\nprint(acct_ab)\n\nfund = int(input())\nacct_ab = acct_ab + fund\nprint(acct_ab)", "solution": "class Account:    \n    def __init__(self, name=\"\", amount=0): \n        self.name = name\n        self.amount = amount    \n\n    def __str__(self):\n        return f\"{self.name}: ${self.amount}\"\n\n    def __add__(self, other):\n        if isinstance(other, Account):\n            return Account(self.name + \", \" + other.name,\n                           self.amount + other.amount)\n        elif isinstance(other, int):\n            return Account(self.name, self.amount + other)\n\nname_a = input()\namt_a = int(input())\nacct_a = Account(name_a,  amt_a)\nname_b = input()\namt_b = int(input())\nacct_b = Account(name_b, amt_b)\n\nacct_ab = acct_a + acct_b # Combine accounts\nprint(acct_ab)\n\nfund = int(input())\nacct_ab = acct_ab + fund\nprint(acct_ab)"}], "input": "Ashe\n4959\nBevins\n8374", "showSolution": true};